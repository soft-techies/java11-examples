pipeline {
    agent any

    stages {
        stage('Install maven') {
            steps {
               sh 'sudo apt  install maven -y'
            }
 stage('jdk') {
            steps {
               sh 'sudo apt install openjdk-21-jdk -y'
            }
        }
    }

